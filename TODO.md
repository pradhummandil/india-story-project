# Phase 0.6 Blockers - Work Plan

- [ ] Fix `RecommendedForYou.tsx`: replace remaining `MODES.map(...)` usage with the derived `modes` array.
- [ ] Fix `story-dna.ts`: remove hardcoded category lookup tables and derive StoryDNA generically/deterministically from actual story fields or hashing.
- [ ] Fix `HeroOfTheDay.tsx`: remove hardcoded timeline titles (Beginning/Turning point/Growth/Momentum) and derive titles from content or omit titles while keeping layout.
- [ ] Run `npm run build` and verify success.
- [ ] Report exactly what changed.

